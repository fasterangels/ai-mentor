## AI Mentor Packaging

This directory contains scripts for building the AI Mentor application into a standalone executable using PyInstaller.

### Build executable

Run the following command from the repository root:

```bash
python packaging/build_app.py
```

### Output

After a successful build, the executable will be available at:

```bash
dist/AI_Mentor
```

